# Datasets (Repo-First Canon)
These CSV files are canonical policy/config datasets used by ZONE-GPT and downstream systems.
Edits must be made via PR and validated by ingestion tooling.
Do not store secrets or PII in datasets.
